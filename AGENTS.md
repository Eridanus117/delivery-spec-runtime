# 共享Spec运行时仓规则

- 本仓是公开共享运行时，只保存 OpenSpec `delivery-change` schema、九个 OMP Commands、TypeScript 工具、合同测试和明确允许的合成或审查后脱敏示例。
- 禁止保存工作或私人 Change、业务 capability specs、账号、凭据、环境、请求响应、trace、run-id、release-id、Speckit、`.specify` 或同义兼容资产。
- 本仓自己的 `openspec/changes/`、`openspec/specs/` 和交付证据只治理 Runtime 自身演进，不属于上一条禁止的工作或私人业务资产；上游 OpenSpec 升级必须经仓内 `delivery-change`、合同验证和 PR。
- TypeScript 通过 `node --experimental-strip-types` 执行；测试使用 Node 内置 `node:test`，不得新增 shell runner。
- runtime 只通过资产仓 `.delivery-spec-runtime` Git submodule 接入；`runtime-manifest.json` 声明四条受管投影（普通文件副本，由 `runtime-check` 逐文件哈希对照 pinned submodule 实时校验）。禁止无校验的副本（理由：查不出漂移的副本会让消费仓悄悄偏离锁定版本）；禁止 `runtime-lock.json` 或任何第二套 commit/hash lock（理由：版本真源只能有一个，两把锁不一致时没人知道哪个算数）。
- 本文件的硬规则须随附一句大白话理由（理由：没有理由的规则，几天后连立法者都无法复审它是否还成立）；既有条目的理由随后续 Change 增补。
- 不创建 Git remote 或 push，除非维护者当轮明确授权。
- GitHub 相关操作（查看、创建、更新、合并 PR，Issue 和 Actions）必须使用 `gh` CLI；禁止改走浏览器，除非维护者明确要求浏览器交互。
- 工件批准记录的 `approvedBy` 必须写明表态的真实形态：亲签、门口表态由 agent 转录、还是证据回填后的机械重批准，一律在该字段本身标注代笔（如「维护者（证据回填后重批准，Claude 代笔）」），不得只把代笔信息藏在 `migrationSource` 里（理由：批准链是给人复核用的，人读第一眼就该分得出这条批准是谁按下的；把代笔降级成附注，等于让一条转录记录长得像亲签）。
- **重批准**（对已批准工件在其 digest 变化后重新签发批准）只在一种情形下合法：**改动是机械回填、不改变工件的任何语义**，典型是把任务状态与验证证据路径回填进 `07-实施任务/实施任务.md`。此时 agent 可以重签，但必须在 `approvedBy` 标注代笔并说明回填性质。**任何改变语义的改动都必须重新取得维护者表态，不得由 agent 重签**（理由：digest 新鲜度机制的全部意义就是「内容变了就要重新过人」；如果 agent 可以自行判断「语义没变」然后重签，这个机制就退化成了一句自述，本仓已经因此被复审判过一次 FAIL）。
- 摆盘深度必须与决策分量匹配，分三档（理由：一律压成一屏会把重裁决摆成无法判断的材料，一律展开又会把例行站位变成阅读负担；深度不是常量，是决策分量的函数）：
  - **例行站位**：从简，甚至只写盘不摆——产物照常落仓库服务审计与 agent 上下文，但不占用维护者的注意力。
  - **两道真门（方案门、验收门）**：一屏。
  - **方向级与复盘级重裁决**：展开说透，允许超一屏。
- 方案门的摆盘必须同时包含「路线比较」与「落地后可感知的具体变化清单」，缺后者即为摆盘不合格（理由：用「权威归代码 vs 归 profile」这类内部结构词描述候选，维护者读不出自己将感知到什么变化，也就无从判断；变化清单要用维护者视角的动作与产物描述——哪些命令会消失、哪些操作以后会被拒绝、哪些文件不再产出、他自己的动作有何增减）。
- 交付流水线校准期继续（2026-08-31 起，至强制版分析线跑满 2 单后的复盘检查点）：维护者对摆盘的反馈**双向记录**——要求展开与要求精简同等入账，一律写进 `openspec/intake/INT-20260831-014-calibration-signals.md` 信号台账，复盘时统一消费（理由：把反馈单向解读为「该站取消人工审阅」会让审阅面只减不增，而信号6 已经证伪过这个单向推断——维护者当场要求的是「说明」，即要求增；只往一个方向读的观察等于没在观察）。
- 资产写盘与人工审阅解耦：写盘服务 agent 上下文与审计，不因某站取消审阅而停写；写盘清单另按「下游是否被消费」裁剪（理由：没人看和没人读是两件事，前者是审阅面问题，后者才是资产问题，用同一把刀裁会误伤审计链）。
- 下一次复盘的触发点与议题已预置：**强制版分析线跑满 2 单后，复盘裁定 A1 的留/修/杀**（理由：A1 取的是「先强制、后评判」，若不预置触发点，「后评判」将永远没有发生的时刻；分析线三单零执行的旧账正是这样欠下的）。
