# 测试计划

## 验证范围与层级

| 层级 | 目标 | 工具/入口 | 通过标准 |
|---|---|---|---|

## 测试环境与数据

| 资产 | 位置 | 生命周期 | 清理责任 |
|---|---|---|---|

## 场景与 Requirement 覆盖

| 场景 ID | Requirement | 前置 | 输入 | 断言 | 清理 |
|---|---|---|---|---|---|

## 自动化资产

```text
06-测试方案/
├── fixtures/
├── scenarios/
├── runtime/
└── runner/*.ts
```

- TypeScript runner 使用 `node --experimental-strip-types`。
- 需求仓是无 remote 的全量本地层，账号、认证信息、环境和输入均按原值保存，不脱敏。
- 每类完整值只保存在一个责任位置，其他文件使用相对路径引用。

## 失败与停止条件

- 

## 数据准备与清理

- 动态资源 ID 由创建步骤捕获。
- 不修改未由本次运行创建的对象。
- 清理失败必须使 08 验收结论为 FAIL。

## 07 任务生成输入

- 列出实现、自动化、失败注入、证据采集和清理所需动作，供 `07-实施任务/实施任务.md` 拆分。
- 本层不写实际运行结果；实际 run-id 证据进入 `08-验收/`。
