# 任务 5.4 验证证据

## 交付物

- `openspec/tools/delivery-control.ts` 新增 `validateEvidence(root, evidence, label)`：逐条证据按 Change 内相对路径解析，依次拒绝绝对路径（含 Windows 盘符形式）、`..` 越界、解析后越出 Change 目录、文件不存在、非普通文件或长度为 0 的空文件。
- 调用点**只接在写入路径**上：`taskWrite`（逐个任务校验导入的 evidence）与 `taskSet`（在 `parseTask` 之后、`atomicWriteJson` 之前）。刻意不放进 `parseTask` / `parseTasks`，因为那两个函数也服务只读解析；放进去会让归档目录的自然语言 evidence 在被读取时直接失效。
- `openspec/contracts/task-state.schema.json`：`evidence` 补 description 说明语义与作用域，并加 pattern 拒绝绝对路径与 `..` 越界。

## 验证

`node --experimental-strip-types --test test/control.test.ts test/contracts.test.ts` → 全部通过。

VC-025（正向）：Change 内存在且非空的 `08-验收/control.tap` 被接受，`task set --state verified` 成功。

VC-026（四类负向，全部 fail closed）：

| 情形 | 输入 | 断言 |
|---|---|---|
| 不存在 | `08-验收/missing.tap` | 非零，`evidence 不存在` |
| 空文件 | `08-验收/empty.tap`（0 字节） | 非零，`evidence 必须是非空文件` |
| 绝对路径 | 临时根的绝对路径 | 非零，`不接受绝对路径` |
| `..` 越界 | `../outside.tap` | 非零，`不得使用 .. 越界` |

每一种情形之后都比对 `task-state.json` 全文，断言与失败前**逐字节相等**——即 fail closed，不写入任何状态。

VC-027（归档不受影响，`test/contracts.test.ts`）：遍历 10 个归档 Change 只读 `inspect`，全部退出码 0；归档目录中持有的自然语言 evidence（如 `PASS`、run-id 描述）不被新校验触及，也不使 `openspec validate --all --strict` 失败（12 passed）。
