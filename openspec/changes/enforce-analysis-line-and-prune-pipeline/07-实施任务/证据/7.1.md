# 任务 7.1 验证证据

## 交付物

- `openspec/specs/intake-workflow/spec.md`：并入 `establish-intake-inventory` 的 4 条需求（逐条原文并入，未改写措辞）：
  1. Inventory SHALL scan only controlled Intake assets
  2. Inventory SHALL report duplicate identities without choosing an authority
  3. Legacy Intake SHALL be visible and non-authoritative
  4. Inventory output SHALL preserve fail-closed boundaries

  长期能力的 Requirement 数由 7 条增至 11 条。
- `openspec/changes/establish-intake-inventory/处置记录.md` 新建：写明未归档成因（与另两个目录同出 commit `5f48c2a`，早于归档纪律，治理证据链从第一环起为空）、spec 归宿逐条列出、delta 目录名与能力名不一致的理由，以及「不补 Review/Acceptance/Readiness——事后补写等于伪造治理证据」。
- `git mv` 至 `openspec/changes/archive/2026-09-01-establish-intake-inventory/`。

并入的是**既成事实**：这 4 条描述的 `intake list` 行为已在 `openspec/tools/intake-control.ts` 实现并被 `test/intake.test.ts` 覆盖（含本轮新增的 VC-021），并入的是规范来源而非新承诺。

## 验证

- `openspec validate --all --strict` → 10 passed / 0 failed（归档后 active Change 由 4 个减为 2 个，故条目数由 12 变 10；零失败）。
- VC-039（`test/contracts.test.ts`）：断言 4 条需求逐条出现在 `openspec/specs/intake-workflow/spec.md`；另断言 `intake list` 的**扫描 / 排序 / 重复 id 报告**三项行为在 specs 中有规范来源（命中「按相对路径稳定排序」与「列出该 ID 和全部冲突文件」）。
- `node --test` 全量 72 passed / 0 failed。

## 一处必须如实记录的重叠

本 Change 自身的 spec delta（`specs/intake-workflow/spec.md`）里 ADDED 了一条 `Runtime SHALL report a deterministic intake inventory`，与本次并入的第 1、2 条在语义上覆盖同一片行为（确定性排序、重复 id 分组、不落盘）。两者**名称不同**，因此 strict validate 不会冲突，本次也未合并它们——本 Change 的 spec 工件已定稿并已获批准，实施层不擅自重开（同 PLAN-010 的处理原则）。该重叠会在 sync 站把 delta 合入长期 spec 时同时存在于 `intake-workflow` 能力中，**建议在 sync 站或下一轮复盘时合并去重**。此处如实登记，不静默吸收。
