# 任务 4.2 验证证据

## 交付物

- `openspec/tools/delivery-control.ts`：删除 `sourcesPath()`、`parseSources()` 与 `main()` 中的 `sources inspect` / `sources write` 两个分支；`JsonObject` 类型导入随之移除。
- 删除 `openspec/contracts/sources.schema.json`。
- `openspec/tools/bootstrap.ts`：不再写 `change-sources.json`，原处留注释说明移除理由。
- `public-allowlist.json`：移除 `openspec/contracts/sources.schema.json` 条目。

归档目录内的既有 `change-sources.json` 一律不动——移除的是写入路径与读取命令，不是历史文件。

## 验证

`node --experimental-strip-types --test test/*.test.ts` → 63 passed / 0 failed。

- VC-022（`test/control.test.ts`）：`sources inspect` 与 `sources write` 均返回非零并报 `未知delivery-control命令`；断言新建 Change 目录下不存在 `change-sources.json`。原用例中「authority 必须连续递增」的断言随合同一并删除，不留死引用。
- VC-038（`test/bootstrap.test.ts`）：原「候选产物 authority 为 [1,2]」的断言改写为「候选产物不含 `change-sources.json`」；bootstrap 的 staged / activate / rollback 语义与受保护树断言全部保持通过。
