# 任务 8.2 验证证据（全量回归）

## 终态

| 项 | 命令 | 结果 |
|---|---|---|
| 合同测试全量 | `node --experimental-strip-types --test test/*.test.ts` | **74 passed / 0 failed**（改造前基线 53） |
| 严格校验 | `openspec validate --all --strict` | **10 passed / 0 failed** |
| 运行时入口 | `runtime-entry.ts runtime-check --change-root .` | `{"allowed": true, ...}` |

`openspec validate` 条目数由基线 12 变为 10，唯一原因是 S7 把两个早期目录移入 `archive/`（归档目录不参与 `--all`）；**零失败**这一点与基线一致。

## VC-031 / VC-032（不得削弱项，与改造前一致）

新增 `test/lifecycle.test.ts`「不得削弱项」用例，把这两条从「基线上没有被改坏」升级为显式断言：

| 校验 | 断言 | 结果 |
|---|---|---|
| `reviewedPaths` 由代码自算 | review-input 传 `reviewedPaths` 报「存在未知字段」 | 通过 |
| 裁决不接受手工指定 | review-input 传 `result` 报「存在未知字段」 | 通过 |
| baseline 祖先校验 | baseline 与 reviewed 对调即非零 | 通过 |
| 写入前 worktree clean | 实现路径脏时报「Review写入前实现路径必须clean」 | 通过 |
| OPEN finding 自动 FAIL | 写入后 `result` 为 FAIL，且 `review inspect` 非零 | 通过 |
| acceptance 四 digest 新鲜度 | `implementation-review.json` / `task-state.json` / `08-验收/验收记录.md` 任一事后改动即 stale；新提交后 implementationCommit 绑定失效 | 四项全部通过 |

即 `implementation-review` 的自算与 `acceptance-state` 的四 digest **一字未动**，本 Change 没有为完成自己而放宽约束 2 保护的两处强校验。

## VC-033（profile 兼容与无额外锁）

- `runtime-check` 通过，四条受管投影哈希对照无漂移。
- `git status --porcelain` 在 Runtime 源仓内无 submodule dirty 项。
- 仓内未新增任何 `runtime-lock.json` 或第二套 commit/hash 清单：本 Change 新增的两份数据文件是 `change-routing-v1.json`（路由表）与 `change-routing.schema.json`（其合同），都不承载版本锁语义。

## 已知环境噪音（不判为回归）

`test/openspec-upgrade.test.ts` 的「升级评估只在临时根生成并输出三类脱敏delta」在本机间歇失败，已登记 `INT-20260831-010`。本轮做过定位：

- 失败信号为某一个消费仓 fixture 的 `runtime-check` 返回 1，报 `运行时 submodule 包含未提交修改`，被判「已修改」的文件恒为 `openspec/changes/archive/2026-08-30-prevent-live-runtime-update-mutation/08-验收/runs/.../outputs/webcoding-spec-runtime-check.json`——一条很长的归档证据路径（与 `INT-20260831-011` 记录的路径预算问题同源）。
- **已排除是本 Change 引入**：把 S2 及之后的改动 `git stash` 回退到 S1 完成态后，同一用例仍以相同签名间歇失败（fail / pass / pass）。
- 按既定处置，复跑两次全绿即视为通过；本轮最终全量运行中该用例通过（74/74）。

`DEP0190` 警告为已知噪音（`INT-20260831-007` 在案），本轮不修不扰。
