# 任务 8.1 验证证据

## 交付物

`public-allowlist.json` 随本 Change 的文件增删同步（83 项）：

| 动作 | 条目 |
|---|---|
| 移除 | `openspec/contracts/sources.schema.json` |
| 移除 | `openspec/contracts/change-mode.schema.json` |
| 移除 | `openspec/schemas/delivery-change/templates/business-current.md` |
| 移除 | `openspec/schemas/delivery-change/templates/technical-current.md` |
| 新增 | `openspec/contracts/change-routing.schema.json` |
| 新增 | `openspec/profiles/change-routing-v1.json` |
| 新增 | `openspec/schemas/delivery-change/templates/current-state.md` |

`test/station-authority.test.ts` 不进表：allowlist 是**公开候选**的文件集合，合同测试整体不在其中（表内原本就没有任何 `test/` 条目）。

## 验证

VC-037（`test/candidate.test.ts` 新增用例「allowlist 与实际文件集合一致」），双向校验：

- **正向**：表内 83 项逐项断言在磁盘上存在，实测 `missing = []`——不留指向已删文件的死条目。
- **反向**：`openspec/contracts`、`openspec/profiles`、`openspec/schemas/delivery-change/templates` 三个目录下的实际 `.json` / `.md` 文件逐项断言已进表，实测三个目录的「未进表」集合均为 `[]`——不漏发。
- **定点**：显式断言本轮删除的两份合同与两份模板**不在**表内、本轮新增的合同与路由表与合并模板**在**表内。

`node --experimental-strip-types --test test/candidate.test.ts` → 4 passed / 0 failed，既有三条（只复制允许清单且无外部副作用、拒绝秘密与禁用路径、示例须有合规 provenance）保持通过。
