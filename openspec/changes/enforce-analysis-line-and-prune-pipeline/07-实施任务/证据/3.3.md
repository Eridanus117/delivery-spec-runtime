# 任务 3.3 验证证据

## 交付物

- `openspec/intake/stages.json`：由五阶段 DAG 改写为两节点模型。声明 `states`（四元）、三个 `exits`、`reopen` 的来源与去向、`requiredSections`（五小节），并单列 `legacy` 段说明 `triaged` 与 `phase` 是只读兼容值、不再由任何命令写入。
- `openspec/contracts/intake-state.schema.json`：`state` 与 `phase` 的 enum 取值保持不变（否则 20 条存量条目会被判失效），改为补 `description` 写明两节点语义与只读兼容定位；新增可选 `changeObject` 字段及其 pattern。
- `openspec/intake/template.md`：五个小节结构原样保留；补 `changeObject` 字段与一段说明注释，讲清「五小节写全后一次处置即可，不需要前置 advance」「phase 只读」「changeObject 供路由表查表」。

保留 enum 中的 `triaged` 而不是删除，是因为仓内确有 1 条存量条目持有该值；删掉会让它在 `list` / `inspect` 时直接解析失败，属于「显式迁移会改写历史治理证据」的同类代价。

## 验证

`node --experimental-strip-types --test test/intake.test.ts` → 12 passed / 0 failed。

VC-021（存量回归，只读）：对仓内真实 `openspec/intake/` 连跑两次 `intake list`，断言

- 两次 stdout 逐字节一致（确定性清单）；
- 条目数 ≥ 19（实际 20，含本轮新登记的 INT-20260901-020）；
- `classification` 全为 `current`——并站没有把任何存量条目判成 legacy 或 invalid；
- `duplicateIds` 为空；
- 全部 `state` 取值落在「四元 + 只读兼容 triaged」范围内；
- 不落盘任何清单文件。

真实台账实测：

```
entries 20 duplicates []
classifications ['current']
states ['captured', 'closed', 'promoted', 'triaged']
```

存量 `triaged` 条目仍被正常读出，`phase` 分布（capture / triage / disposition）不受影响。
