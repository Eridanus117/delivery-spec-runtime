# 发布计划与快速资产

## Change 模式与发布结论

- 模式：delivery
- 结论：GO
- 授权：仓库维护者在当前会话批准方案并要求继续；发布载体限定为功能分支与 PR。

## 08 验收门禁

- [x] `08-验收/验收记录.md` 结论为严格 PASS。
- [x] PASS run-id `20260830T1114Z-runtime-safety` 覆盖全部必需 Requirement 和清理结果。
- [x] Runtime 完整合同 15/15 PASS，三个消费仓只读 smoke 全部通过。

## 发布范围

| 仓库/能力 | Commit/版本 | 环境 | 责任方 |
|---|---|---|---|
| `Eridanus117/delivery-spec-runtime` | PR #1 当前功能分支 head | GitHub `master` | Runtime 维护者 |
| `runtime-entry.ts runtime-update` | fail closed | 所有后续升级 Runtime gitlink 的消费仓 | 各消费仓维护者 |
| `/opsx-update` 与 Runtime README | 受控升级说明 | Runtime 文档消费者 | Runtime 维护者 |

本 Change 不更新三个消费仓的 gitlink；消费仓只在 Runtime PR 合并后按各自发布节奏升级。

## 发布顺序与依赖

| 顺序 | 外部平台/人工动作 | 前置 | 成功标准 | 失败处置 |
|---:|---|---|---|---|
| 1 | 推送 `change/prevent-live-runtime-update-mutation` | 严格验收 PASS | 远端功能分支存在，默认分支未直接写入 | 停止，不创建或合并 PR |
| 2 | 审阅 PR #1 | PR head 包含 08/09 证据 | diff 仅包含本 Change 范围，必需检查针对同一 head 通过 | 修复后使用新 head 重新验证 |
| 3 | 合并 PR #1 | 当前 head 与通过检查的 head 一致 | `master` 包含安全修复提交 | 立即停止消费仓升级；必要时 revert 合并提交 |
| 4 | 消费仓按需更新 gitlink | Runtime 默认分支修复已稳定 | `runtime-check` 与消费仓验证通过 | 回退消费仓 gitlink 到上一 commit |

## 现场快速资产

| 资产 | 路径/系统 | 用途 | 责任方 | 使用时点 |
|---|---|---|---|---|
| Runtime 合同测试输出 | `08-验收/runs/20260830T1114Z-runtime-safety/outputs/runtime-tests.tap` | 合并前核对行为 | Runtime 维护者 | PR 检查 |
| 消费仓 smoke 输出 | `08-验收/runs/20260830T1114Z-runtime-safety/outputs/*-runtime-check.json` | 核对现有绑定无新增失败 | Runtime 维护者 | PR 检查 |
| Requirement 映射 | `08-验收/runs/20260830T1114Z-runtime-safety/traces/requirement-coverage.md` | 验收追踪 | Runtime 维护者 | 审阅与归档 |
| 回滚动作 | 本文件“停止与回滚” | 失败处置 | Runtime/消费仓维护者 | 出现停止条件时 |

## 日志、指标与观察窗口

| 指标/日志 | 基线 | 告警/停止条件 | 观察窗口 |
|---|---|---|---|
| Runtime 合同测试 | 15/15 PASS | 任意失败 | PR 合并前每个新 head |
| 消费仓 `runtime-check` | 三仓均 `allowed: true` | 任一仓新增失败 | PR 合并前及首批消费仓升级时 |
| Runtime submodule dirty 状态 | 拒绝路径保持 clean | Commands 摘要变化或父仓/submodule dirty | 首批消费仓升级验证 |

## 配置开关

无配置开关。本 Change 是入口级 fail-closed 行为，不设置兼容开关或回退开关。

## 停止与回滚

- 停止条件：PR 必需检查失败；PR head 与已验证 head 不一致；任一消费仓 `runtime-check` 新增失败；拒绝路径改变 Commands 摘要、git 状态或软链目标。
- 回滚门槛：合并后发现正常 lifecycle Command 被误阻断，或消费仓无法继续使用锁定 Runtime。
- 回滚顺序：停止消费仓 gitlink 升级；revert Runtime 合并提交；运行 Runtime 完整合同；将已升级消费仓 gitlink 回退到上一已知正常 commit。
- 回滚后验证：Runtime 完整合同通过；三个消费仓 `runtime-check` 通过；Runtime submodule 和父仓 gitlink 状态 clean。

## 外部发布记录

当前仅创建 PR #1，尚未合并，未生成成功 release-id。合并和观察完成后使用独立 `release-id` 记录实际 head、检查、合并结果与消费仓观察；失败或重试不得覆盖。

## 归档门禁

- [ ] PR #1 已合并，实际 release-id 已保存。
- [ ] 观察窗口完成，首批消费仓升级无新增失败。
- [ ] `/opsx-verify` 无 critical。

归档门禁尚未满足；本 Change 随 PR 交付，但此时不得 sync 或 archive。
