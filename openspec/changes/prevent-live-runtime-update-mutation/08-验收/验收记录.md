# 验收

## Change 模式

- 模式：delivery
- 模式来源：未设置 `change-mode.json`，按合同使用 delivery。
- rehearsal 原因与批准：不适用。

## 前置门禁

- [x] `07-实施任务/实施任务.md` 六项任务全部为 `verified`。
- [x] 实际 commit、Node/OpenSpec 版本和目标环境已记录在 run metadata。
- [x] `06-测试方案/测试方案.md` 的必需场景、fixture、断言和清理规则均已执行。
- [x] `delivery-control.ts guard acceptance` 返回 `allowed: true`。

## 验收对象

| 仓库/制品 | 实际 Commit/版本 | 目标环境 | 依赖 |
|---|---|---|---|
| `Eridanus117/delivery-spec-runtime` | `ae18b997c8eccbc7e4cf5bc3744ba9b49893f63c` | macOS arm64 本机合同环境 | Node `v24.7.0`、OpenSpec `1.10.0` |
| `agent-system` Runtime 绑定 | 父仓锁定版本 | 只读 smoke | 已初始化 `.delivery-spec-runtime` |
| `webcoding-spec` Runtime 绑定 | 父仓锁定版本 | 只读 smoke | 已初始化 `.delivery-spec-runtime` |
| `work-spec` Runtime 绑定 | 父仓锁定版本 | 只读 smoke | 已初始化 `.delivery-spec-runtime` |

## Requirement 覆盖与结论

| Requirement/Scenario | Run ID | 证据路径 | 结果 | 偏差 |
|---|---|---|---|---|
| 实时资产仓必须拒绝 Runtime 升级生成 | `20260830T1114Z-runtime-safety` | `runs/20260830T1114Z-runtime-safety/outputs/runtime-tests.tap` | PASS | 无 |
| 拒绝路径不得改变九个 Commands 摘要、submodule/父仓状态和三条软链 | `20260830T1114Z-runtime-safety` | `runs/20260830T1114Z-runtime-safety/outputs/runtime-tests.tap` | PASS | 无 |
| 官方 OpenSpec 可用时仍在生成前拒绝 | `20260830T1114Z-runtime-safety` | `runs/20260830T1114Z-runtime-safety/metadata.json` | PASS | 无 |
| `/opsx-update` 必须说明受控升级失败语义 | `20260830T1114Z-runtime-safety` | `runs/20260830T1114Z-runtime-safety/outputs/runtime-tests.tap` | PASS | 无 |
| 三个消费仓不得出现新增 Runtime 合同失败 | `20260830T1114Z-runtime-safety` | `runs/20260830T1114Z-runtime-safety/outputs/*-runtime-check.json` | PASS | 无 |

## 验收运行

实际运行目录：`08-验收/runs/20260830T1114Z-runtime-safety/`

- `metadata.json`：commit、环境、版本、命令、时间和退出状态。
- `inputs/commands.txt`：实际执行命令。
- `outputs/runtime-tests.tap`：Runtime 15 项合同测试原始输出。
- `outputs/*-runtime-check.json`：三个消费仓只读 smoke 原始输出。
- `traces/requirement-coverage.md`：Requirement 与证据映射。
- `cleanup/cleanup.md`：临时 fixture 和外部资源清理结果。
- `conclusion.md`：本次 run 的独立结论。

## Critical 与清理

- 未解决 critical：无。
- 清理结果：测试 fixture 全部在 `finally` 删除；消费仓只执行只读 `runtime-check`；无外部临时资源残留。

## 最终验收结论

结论: PASS

依据：Runtime 完整合同 15/15 PASS；新增缺陷测试已证明旧实现失败、新实现通过；三个消费仓 smoke 全部 `allowed: true`；无偏差、无未解决 critical，清理成功。
