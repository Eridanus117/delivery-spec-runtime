# 方案决策

- 状态：APPROVED
- 选择：候选 B——实时仓硬拒绝，升级只在 Runtime 独立 Change 中执行
- 决策人：仓库维护者（由已合并 PR #1 和当前历史迁移确认）
- 决策时间：2026-08-30T11:21:18Z
- 证据：PR #1、原 `改造方案.md`、缺陷回归、08 验收记录和 merge commit `b8efa99f4adf3d735e19007b2ab3d9739fead433`

## 接受的后果

- 消费仓和实时资产仓不能直接触发 OpenSpec Runtime 重生成。
- 维护者也没有隐藏绕过开关；升级必须建立 Runtime 独立 Change。
- `/opsx-update` 必须明确区分修订 Change 产物与升级 Runtime。
- 消费仓 gitlink 采用由各消费仓独立 Change 管理，不阻塞 Runtime Archive。

## 拒绝方案

- 拒绝候选 A：备份恢复不能覆盖共享 submodule、软链和并发失败的全部风险。
- 拒绝候选 C：维护者绕过入口破坏 fail-closed 不变量并形成隐蔽第二路径。
