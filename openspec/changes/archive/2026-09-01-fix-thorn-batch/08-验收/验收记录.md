# 验收

## 前置门禁

- [x] `task-state.json` 全部 verified（14/14），07 投影一致（`task render` 生成，`guard verify` 复核）。
- [x] `implementation-review.json` 为当前、PASS，且没有 OPEN finding（11 条：9 条 RESOLVED、2 条 ACCEPTED）。
- [x] 实际 implementation commit、依赖版本和目标环境已锁定，06 必需场景可执行。

## 验收对象

| 仓库/制品 | 实际 Commit/版本 | 目标环境 | 依赖 |
|---|---|---|---|
| delivery-spec-runtime | 实现 `108d9cc`；基线 `cd6d2f0` | Windows 11 Pro 10.0.26200，工作树原地 | Node 25.2.1（`--experimental-strip-types`）、OpenSpec 1.11.0、git（含 `core.longpaths` 能力） |
| 受管投影（随本 Change 变更） | `openspec/tools/runtime-entry.ts` | 消费仓经 gitlink 升级后获得 | 由各消费仓独立 Change 管理，不阻塞本 Change 归档 |

## Requirement 覆盖与结论

| Requirement/Scenario | Run ID | 证据路径 | 结果 | 偏差 |
|---|---|---|---|---|
| 受管投影的校验 SHALL 覆盖「本机可见但入不了库」的坏账（3 场景） | 20260901T1330Z-thorn | `outputs/full-tests.log`（T-IGN-1/2、T-MODE-1） | PASS | 无 |
| 路径长度 SHALL NOT 影响 Runtime 入口的 git 判定（2 场景） | 同上 | `outputs/full-tests.log`（T-LONG-1/2）、`traces/neg-controls-longpath.log` | PASS | 无 |
| Runtime 分发的命令文档 SHALL 在消费仓侧逐字可执行（2 场景） | 同上 | `outputs/full-tests.log`（T-ENTRY-1/2、T-GUARD-2） | PASS | 无 |
| 消费仓冒烟失败 SHALL 在报告中留下可归因的原因（2 场景） | 同上 | `outputs/full-tests.log`（T-SMOKE-1/2、T-COMPAT-1） | PASS | 无 |
| Runtime CLI 的输出 SHALL 不含运行时弃用告警（1 场景） | 同上 | `outputs/full-tests.log`（T-QUIET-1） | PASS | 无 |
| 仓内证据路径 SHALL 受可机器校验的长度预算约束（2 场景） | 同上 | `outputs/full-tests.log`（T-PATH-1/2，`VC-041`） | PASS | 无 |

三份 delta spec 共 6 个 Requirement、12 个 Scenario，全部有断言承载且本轮实测通过。
断言与 Requirement 的对应关系见 `06-测试方案/000-测试方案索引.md` 的场景表。

## 验收运行

```text
08-验收/runs/20260901T1330Z-thorn/
├── metadata.json
├── inputs/README.md
├── outputs/{full-tests,render-check,runtime-check,guard-verify,validate}.log
├── traces/neg-controls-longpath.log
└── cleanup/cleanup-pass.md
```

终态数字：**全量合同测试 90/90，连跑三次全绿**（基线 79 项，本单新增十一组断言）；
`render-commands check` 0 漂移；`runtime-check` allowed；`guard verify` allowed；
`openspec validate --all --strict` 10/10。

## 交付内容

**十根刺 + 三类清理**（范围条目引用各自 intake 台账，正文不复制）：

| # | 刺 | 处置 |
|---|---|---|
| 1 | `INT-20260831-009` CRLF 渲染漂移 | 按裁定 #2 **不扩** `.gitattributes`；核查发现该条目另一半（行尾归一化升格为 spec 显式要求）早已落地 |
| 2 | `INT-20260831-010` 升级冒烟偶发失败 | 归因由「git racy 竞态」实测改判为 Windows 路径越限；`runtime-entry.ts` 的 git 调用补 `core.longpaths` |
| 3 | `INT-20260831-011` 归档证据路径预算 | `VC-041` 把预算冻结在实测最长值 155 并与之绑死，超限点名 |
| 4 | `INT-20260831-012` Windows temp EPERM | 临时目录清理统一改用有限退避（`maxRetries` + `retryDelay`） |
| 5 | `INT-20260831-016` 消费仓 ignore 吞投影 | `runtime-check` 对未跟踪的投影文件跑 `git check-ignore`，命中即 fail-closed |
| 6 | `INT-20260831-017` index 软链模式坏账 | `runtime-check` 断言投影在 index 中的模式不得为 `120000` |
| 7 | `INT-20260831-018` 投影副本不可作入口 | 按裁定 #1「算可执行入口」，`sourceRootFromScript()` 回落 `findConsumerRoot()`；`.omp/commands` 一字未改 |
| 8 | `INT-20260831-007` 遗留的 DEP0190 噪音 | 版本探测去 `shell` 选项，输出恢复干净 |
| 9 | 冒烟失败不可归因（实施期从 #2 分离出的独立缺陷） | `consumerSmoke` 保留 stderr 写进报告，合同容纳新旧两种形状 |
| 10 | `INT-20260901-024` 两处入口守卫（范围变更二） | `delivery-lifecycle.ts` 与 `render-commands.ts` 的判据改用 `samePath()` |
| A | 未用导入 | 删除 6 处（含终审前补删的 `test/bootstrap.test.ts` 的 `statSync`） |
| B | 零调用导出 | 删除 `runtime-lib.ts` 的 `findUp` / `gitCommit` / `ensureInside` |
| C | 声明后未使用的局部变量 | `openspec-upgrade.ts` 的 `afterLinkStatus` 被赋予用途（并入冒烟失败原因） |

## 四轮评审轨迹

| 轮次 | 评审 commit | 结论 | finding |
|---|---|---|---|
| 一（实施评审） | `ea8d197` | FAIL | REV-001/002 MEDIUM、REV-003/004/005 LOW 判 OPEN；REV-006/007 LOW 判 ACCEPTED |
| 二（收口复审） | `bdc771f` | FAIL | 首轮 7 条全关；新增 REV-008 **CRITICAL**、REV-009 MEDIUM |
| 三（终审） | `bc630e5` | PASS | 11 条全部关闭；新增 REV-010/011 LOW 判 ACCEPTED |
| 四（归档前机械订正） | 本次 | — | 顺手落地 REV-011 的计数订正；REV-010 的 INT-024 勘误留到归档后单独提交（改 `openspec/intake/**` 会让已 PASS 的 review 失新鲜） |

最重的一条是 **REV-008（CRITICAL）**：首轮返工为让测试能 import 判据，给入口加了
「仅直接执行时才跑 `main()`」的守卫，判据是 `argv[1]` 与 `import.meta.filename` 的路径比较。
Node 的 ESM 加载器对主模块解析软链与 junction，两者在路径含软链时必然不等——
全体消费仓唯一的 fail-closed 闸门会以退出码 0、零输出静默跳过。已删除守卫并由
`VC-042` 与 `T-GUARD-2` 两条断言钉死不得回潮。

## 范围两次变更的裁定记录

**变更一（实施期，D-05）：条目档位由 `tool-code` 升为 `governance-contract`。**
裁定 #3 要求把冒烟失败原因写进升级报告，报告新增字段必须同步
`openspec/contracts/openspec-upgrade-report.schema.json`，该路径按路由表属治理合同档（序 30），
高于原声明 `tool-code`（序 20）。路由表规定的处置就是「修正条目的 `changeObject` 声明」，
并明文禁止「缩小改动面以迁就声明」。两档要求的分析 profile 相同，已完成的分析线对新档位同样满足，
交付档位仍为 `delivery-change`，立项门结论不变。实证：声明改回 `tool-code` 时
`guard verify` 当场点名该路径并拒绝。

**变更二（收口期，D-09）：`INT-20260901-024` 的两处提前到本批修。**
该条目登记时逐条实跑，发现三处基线守卫中两处的爆炸半径高于评审最初「都是维护者侧内部工具」的评估——
`delivery-lifecycle.ts` 在消费仓治理链上（入口的 `lifecycle` 子命令以未经 realpath 的
`runtimeRoot` 拼路径 spawn 它），`render-commands.ts` 是 CI 的「Check rendered Commands」步骤。
维护者采纳该订正后变更裁定：两处提前到本批修（消费仓治理链与 CI 检查不容许已知的静默哑炮），
`openspec-upgrade.ts` 一处维持登记不修。

## 分析线首跑实况（本单的另一项产出）

本单是强制版分析线生效后的**第一单真实运行**。五阶段逐站推进并做了负向探针：
缺输入报 `blocked` 并点名、补输入不给判断报 `waiting_human_judgment`、给判断即推进，
立项门在 `status=completed` 且 `disposition=build` 后放行——**门禁全部如设计生效，无一处需要绕过，也无一次挡错**。
分析线的实际产出是可验证的：`discover` 站的强制取证推翻了 `INT-20260831-010` 挂了一整天的
错误归因，修法方向随之从「加重试」改为「治路径能力与路径长度」。
使用成本、门禁缺口与这条读数已写入 `openspec/intake/INT-20260831-014-calibration-signals.md` 信号9，
供「强制版分析线跑满 2 单后复盘裁定 A1」时消费。

## Critical 与清理

- 未解决 critical：无。REV-008（CRITICAL）已 RESOLVED，并有 `VC-042` 与 `T-GUARD-2` 两条回归断言守住。
- 清理结果：PASS，见 `08-验收/runs/20260901T1330Z-thorn/cleanup/cleanup-pass.md`。

## 最终验收结论

- 结论：PASS
- 依据：三份 delta spec 的 6 个 Requirement、12 个 Scenario 全部有断言承载并实测通过；
  全量测试连跑三次 90/90；四项校验（`runtime-check`、`render check`、`guard verify`、
  `validate --all --strict`）全绿；`implementation-review.json` 为当前、PASS、无 OPEN finding；
  清理证据 PASS；维护者已于 2026-09-01 主会话在验收门表态「同意」。
- 只有必需场景有证据、无 critical 且清理成功时才能填写 PASS。

## Acceptance State

正文和证据完成后执行 `delivery-lifecycle.ts acceptance write`。只有生成的
`acceptance-state.json` 仍绑定当前 Review、task state、implementation commit 和本文件摘要时，09 才能继续。
