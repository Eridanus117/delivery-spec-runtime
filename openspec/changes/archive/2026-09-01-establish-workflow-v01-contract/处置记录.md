# 处置记录：归档（superseded）

- 处置时间：2026-09-01
- 处置依据：`openspec/intake/INT-20260831-019-three-item-retrospective.md` 裁定 C4；由 Change `enforce-analysis-line-and-prune-pipeline` 的切片 S7 执行。
- 处置结论：**标 superseded 直接归档，不补验收**。

## 取代关系

本 Change 的目标（建立 Workflow System V0.1 的调用合同）已被后续工作完整取代：

- **实现侧**：commit `5daf1bd`「建立可自托管的多 Profile Workflow Runtime (#9)」交付了多 Profile 的 workflow runtime，覆盖并超出本 Change 计划中的 V0.1 单 profile 调用合同。
- **规范侧**：长期能力 `openspec/specs/workflow-profiles/spec.md` 承接了 profile 注册、版本精确匹配、阶段推进与人工判断门的正式 Requirement，本 Change 的 delta 无独占内容可并入。

因此本目录不再有可交付的增量，其 `task-state.json` 中长期停在 `planned` 的条目将**永久保持未完成**，作为历史记录而非待办。

## 明确保留的资产

`openspec/tools/workflow-entry.ts` **不随本 Change 归档而移除**。它是 standalone workflow 的入口，仍在 `public-allowlist.json`、`test/workflow.test.ts` 的断言与两份 docs 中被引用；归档的是这份未完成的规划记录，不是它规划过的代码。

## 未补的环节

不补 Review / Acceptance / Readiness：本 Change 从未进入实施，补写验收等于为没有发生的交付伪造证据。
