# 发布计划与快速资产

## 交付结论

- 结论：GO
- GO 表示 Change 已准备在功能分支完成 Spec Sync 和 Archive；不表示 PR 已创建或合并。

## 08 验收门禁

- [x] 当前 `acceptance-state.json` 为 PASS。
- [x] 至少一个 PASS run-id 覆盖必需 Requirement 和清理结果（`20260901T035020Z`）。

## 发布范围

| 仓库/能力 | Commit/版本 | 环境 | 责任方 |
|---|---|---|---|
| delivery-spec-runtime（强制分析线立项门、登记并站、流水线修剪、站位权威归真门禁） | `a70ed5d` 之后的功能分支 `Eridanus117/int015-closeout` | Windows 11 / Node 25.2.1 / OpenSpec 1.11.0 | Runtime 维护者 |

## 发布顺序与依赖

| 顺序 | 外部平台/人工动作 | 前置 | 成功标准 | 失败处置 |
|---:|---|---|---|---|
| 1 | Spec Sync：4 份 delta 合入长期 spec | acceptance PASS | 17 条 Requirement 全部落位，strict validate 全绿 | 回退 `openspec/specs/` 改动，重跑合并 |
| 2 | Archive：Change 移入 `openspec/changes/archive/` | `guard archive` 放行 | 归档目录就位，validate 计数相应下降且零失败 | `git mv` 反向还原 |
| 3 | 本地 commit 并 push 功能分支 | 全量终验绿 | 分支推送成功 | 停止并报告 |
| 4 | 用 `gh` 开 PR 到 main（不合并） | push 完成 | PR 创建成功并回报编号 | 停止并报告 |
| 5 | 消费仓 `agent-system` main 推送 | 确认本地领先 origin 恰一提交且无他人新提交 | 推送成功 | 有冲突即停止并报告，不强推 |

说明：第 3–5 步已获维护者在验收门明确授权（「同意，授权」，2026-09-01 主会话）。

## 停止与回滚

- 停止条件：全量测试出现非已知 flake 的失败；strict validate 非零；`guard archive` 拒绝；消费仓 push 前发现他人新提交。
- 回滚门槛：归档后发现门禁行为回归。
- 回滚顺序：`git revert` 功能分支上的归档提交 → 还原 `openspec/specs/` → Change 目录 `git mv` 回 active。
- 回滚后验证：`node --test`、`openspec validate --all --strict`、`runtime-check` 三项复绿。

## Spec Sync 与归档准备

| Delta Spec | Main Spec | Strict Validation | 结果 |
|---|---|---|---|
| `specs/delivery-lifecycle-governance/spec.md` | `openspec/specs/delivery-lifecycle-governance/spec.md` | PASS | 2 MODIFIED + 5 ADDED |
| `specs/human-interaction/spec.md` | `openspec/specs/human-interaction/spec.md` | PASS | 1 MODIFIED + 1 ADDED |
| `specs/intake-workflow/spec.md` | `openspec/specs/intake-workflow/spec.md` | PASS | Purpose 更新 + 4 MODIFIED + 2 ADDED |
| `specs/workflow-profiles/spec.md` | `openspec/specs/workflow-profiles/spec.md` | PASS | 1 MODIFIED + 1 ADDED |

- [x] 所有 delta specs 已同步到 `openspec/specs`，没有待应用差异。
- [x] Change 与主 specs strict validate 全部通过。
- [x] cleanup 证据存在且结论 PASS。
- [x] 最终 PR 尚未创建；维护者将通过 `archive-readiness.json` 作 `prStarted=false` 声明。

## Archive Readiness

使用 `delivery-lifecycle.ts readiness write` 绑定 Acceptance、本文件、Spec Sync 输入输出和 cleanup
摘要。只有 `guard archive` 返回 allowed 才能移动 Change；不得再以 `release-id`、
`release-not-required`、任意关键词或人工复选框替代机器状态。

本 Change 起 `attestedBy` 由 `acceptance-state.json` 的 `acceptedBy` 派生，归档不再索取第二次人工表态——
验收的「同意」即为归档授权。

## 归档后 PR

1. 在功能分支归档 Change。
2. 对归档后的仓库执行 final strict validation 和完整测试。
3. final validation 通过后才创建 PR。
4. PR 反馈若要求改变实现或规格，停止合并并受控 reopen，重新 Review→Acceptance→Sync→Archive。
5. 消费仓 gitlink 更新由各消费仓独立 Change 管理，不阻塞 Runtime Archive。

决策依据分别保存在 `05-改造方案/方案提案.md` 和 `05-改造方案/方案决策.md`。
