# 任务 1.1 验证证据

## 交付物

- `openspec/profiles/delivery-change-v1.json`：新增 `definitionAuthority`（`kind: runtime-code`，指向 `delivery-control.ts` 与 `delivery-lifecycle.ts`），`archive` 站 `humanJudgment` 由 `true` 改为 `false` 并在 description 写明「验收的同意即为归档授权」。
- `openspec/tools/workflow-core.ts`：`WorkflowProfile` 增可选 `definitionAuthority`；`parseWorkflowProfile` 的允许键扩充该键；新增 `parseDefinitionAuthority` 校验 `kind` 枚举、`paths` 非空且为仓库内相对路径不重复、`note` 非空。
- `openspec/contracts/workflow-profile.schema.json`：同步 `definitionAuthority` 定义（`additionalProperties: false`）。

## 验证

`node --experimental-strip-types --test test/workflow.test.ts` → 12 passed / 0 failed。
其中既有断言「profile 合同拒绝重复阶段和未知字段」保持通过，说明扩键未放宽未知键拒绝。

profile 解析结果（`delivery-change-v1.json`）：

```
definitionAuthority = {"kind":"runtime-code","paths":["openspec/tools/delivery-control.ts","openspec/tools/delivery-lifecycle.ts"],"note":"..."}
proposal=false decision=true implementation=false review=false acceptance=true sync=false archive=false
```

`archive` 已不再标人工判断，人工门由 4 个正名为 3 个（decision / acceptance）。站位 id 与顺序未变。
