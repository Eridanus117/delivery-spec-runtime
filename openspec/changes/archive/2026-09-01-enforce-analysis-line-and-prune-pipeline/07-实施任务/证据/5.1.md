# 任务 5.1 验证证据

## 交付物

- `openspec/schemas/delivery-change/schema.yaml`：`version: 5` → `6`；`business-current` 与 `technical-current` 两个 artifact 合并为 `current-state`（`generates: 03-现状/现状.md`，`template: current-state.md`），instruction 合并两面的写作要求并保留「区分源码事实、运行态事实和推断」「不得把目标方案写成当前事实」。下游 `requires` 中对两者的引用统一改为 `current-state`（去重后只留一条）。
- `openspec/schemas/delivery-change/templates/current-state.md` 新建：把原两份模板的全部小节并入一份，业务面（调查口径、角色与责任、当前业务流程、业务对象与状态、异常与失败分支）与技术面（基线 commit 表、当前技术流程、接口与数据边界、事务缓存通知兼容、部署与运行态）都保留，两张 Mermaid 图分别保留。
- 删除 `templates/business-current.md` 与 `templates/technical-current.md`。
- 顺带清理：`schema.yaml` 的 acceptance / release / apply instruction 中的 rehearsal 表述已随 change-mode 概念移除，并补写「readiness 的 attestedBy 由 acceptance-state 的 acceptedBy 派生，归档不再索取第二次人工表态」。

## 验证

- VC-029（`test/contracts.test.ts`）：遍历全部 10 个归档 Change 逐个 `delivery-control inspect`，断言仍按 **v5** 结构解析（`effective` 键集合恰为含 `business-current` / `technical-current` 的九项），退出码 0。旧结构 Change 不因 v6 判失效。
- `openspec validate --all --strict` → 12 passed / 0 failed，与改造前基线一致。
- `test/contracts.test.ts` 的 schema 版本断言由 `version: 5` 改为 `version: 6`，目录清单断言由 `03-业务现状` + `04-技术现状` 改为 `03-现状`。

存量不迁移：10 个归档 Change 与本 Change 自身保持 v5 结构，分界线是 Change 目录形状本身（有 `03-现状/现状.md` 即 v6）。
