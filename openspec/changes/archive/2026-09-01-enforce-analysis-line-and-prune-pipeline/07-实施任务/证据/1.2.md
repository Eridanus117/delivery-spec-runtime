# 任务 1.2 验证证据

## 交付物

- `openspec/tools/delivery-lifecycle.ts` `readinessWrite`：
  - `readiness-input` 的允许键与必需键同时移除 `attestedBy` / `attestedAt`（两处 exactKeys 参数）。
  - `attestedBy` 改为 `acceptance.acceptedBy` 派生；`attestedAt` 改为写入时刻 `now()`。
  - `requireLaterTimestamp(attestedAt, acceptance.acceptedAt)` 保留不动。
- `openspec/contracts/archive-readiness.schema.json`：`attestedBy` / `attestedAt` 补 `description`，写明是派生字段、`readiness write` 不接受其作为输入；存储侧 required 清单不变（13 个字段全在）。
- `test/lifecycle.test.ts`：readiness-input 去掉两键；新增 VC-004 与 VC-005 断言。

## 验证

`node --experimental-strip-types --test test/lifecycle.test.ts` → 2 passed / 0 failed。

### VC-004（归档不再索取第二次人工表态）

- 不含 `attestedBy` / `attestedAt` 的输入写入成功。
- 断言 `archive-readiness.json` 的 `attestedBy` 全等于 `acceptance-state.json` 的 `acceptedBy`。
- 断言 `Date.parse(attestedAt) > Date.parse(acceptedAt)`。
- 负向：分别塞入 `attestedBy` 与 `attestedAt` 两键，均非零并报 `readiness-input 存在未知字段 <key>`，即输入侧无法再手工指定表态人。

### VC-005（门禁条目一项不减，五种破坏情形 archive guard 全部非零）

| # | 破坏情形 | 断言位置 | 期望 |
|---|---|---|---|
| 1 | cleanup 证据非 PASS | `readiness write` | 非零，`/cleanupEvidence.*PASS/` |
| 2 | acceptance stale（task-state 事后改动） | `acceptance inspect` | 非零，`/stale/` |
| 3 | specSync 摘要漂移 | `readiness inspect` | 非零，`/spec sync stale/` |
| 4 | releasePlan 事后改动 | `guard --operation archive` | 非零，`/release plan stale/` |
| 5 | `prStarted=true` 且非历史迁移 | `readiness write` | 非零，`/prStarted=false/` |

破坏后逐项复原，末次 `guard --operation archive` 仍返回 0，证明门禁未被这次改造放宽。
