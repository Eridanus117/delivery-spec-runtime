# 任务 7.2 验证证据

## 交付物

- `openspec/changes/establish-workflow-v01-contract/处置记录.md` 新建，写明取代关系：
  - **实现侧**由 commit `5daf1bd`「建立可自托管的多 Profile Workflow Runtime (#9)」取代——它交付的多 Profile workflow runtime 覆盖并超出本 Change 计划中的 V0.1 单 profile 调用合同。
  - **规范侧**由长期能力 `openspec/specs/workflow-profiles/spec.md` 承接，本 Change 的 delta 无独占内容可并入。
  - 明确记录其 `task-state.json` 中长期停在 `planned` 的条目将**永久保持未完成**，作为历史记录而非待办。
  - 明确记录不补 Review / Acceptance / Readiness 的理由：本 Change 从未进入实施，补写验收等于为没有发生的交付伪造证据。
- `git mv` 至 `openspec/changes/archive/2026-09-01-establish-workflow-v01-contract/`。

## 验证

VC-040（`test/workflow.test.ts`，新增用例「归档 establish-workflow-v01-contract 不连带移除 workflow-entry.ts」）：

| 断言 | 实测 |
|---|---|
| `openspec/tools/workflow-entry.ts` 文件仍存在 | 通过 |
| 仍在 `public-allowlist.json` 中 | 1 处 |
| 仍被 `test/workflow.test.ts` 的断言引用（要求 ≥ 6 处） | 实测 8 处 |
| 仍被两份 docs 引用 | `docs/architecture.md`、`docs/workflow-guide.md` 各 1 处 |

归档的是那份未完成的**规划记录**，不是它规划过的**代码**——这条区分被写进用例注释，防止后来者按目录名连带清理。

VC-039（`test/contracts.test.ts`）：断言 `openspec/changes/` 下的 active 目录恰为 `["enforce-analysis-line-and-prune-pipeline", "establish-runtime-metrics-baseline"]`；两个归档目录存在且各带 `处置记录.md`；superseded 记录必须命中 `superseded`、`5daf1bd`、`workflow-profiles` 三个关键指针。

`openspec validate --all --strict` → 10 passed / 0 failed。`establish-runtime-metrics-baseline` 按裁定 C3 未动。
