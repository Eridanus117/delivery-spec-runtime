# 任务 2.1 验证证据

## 交付物

- `openspec/contracts/change-routing.schema.json`（新建）：`additionalProperties: false`，要求 `schemaVersion` / `routingVersion` / `unmatched` / `routes`。`unmatched.profileId` 与 `unmatched.requiresAnalysis` 用 `const` 钉死为 `delivery-change` 与 `true`——兜底档位不可配置成豁免，否则「未匹配取最重档」这条规则会被一次配置改动绕过。
- `openspec/profiles/change-routing-v1.json`（新建）：按改动对象四分类，每行声明交付档位、是否必走分析线与理由。

| changeObject | 档位 | 必走分析线 |
|---|---|---|
| governance-contract | delivery-change | 是 |
| tool-code | delivery-change | 是 |
| doc-expression | light-change | 否 |
| ledger-only | light-change | 否 |

至少一条必走行（governance-contract / tool-code）与一条豁免行（doc-expression / ledger-only），满足 S2 独立验收要求。

## 验证

`node --experimental-strip-types --test test/intake.test.ts` → 9 passed / 0 failed。

- VC-012（未匹配取最重档）：`intake promote` 对表内没有的 `something-not-in-table` 与完全未声明 `changeObject` 的存量条目，均判为 `delivery-change` 且不豁免，因缺分析线产物被挡。
- VC-014（豁免行生效）：`doc-expression` 条目无任何分析线产物仍放行，输出 `routing.matched=true`、`requiresAnalysis=false`、`profileId=light-change`。
- 「仓内路由表满足立项门的结构不变量」用例直接读仓内表断言：`unmatched` 为最重档且不豁免、至少各含一条必走与豁免行、`changeObject` 不重复、每行都有 `reason`、豁免行只允许出现在 `doc-expression` / `ledger-only` 上。最后一条防止后来者把 `tool-code` 悄悄改成豁免。

---

> **返工指针**：REV-002 / REV-006 返工：路由表增 rank + pathPrefixes（供 guard verify 交叉校验）、analysisProfileId（指定分析须由哪个 profile 产出）、promotable（ledger-only 不可立项）。 逐条处置见 07-实施任务/证据/REV-返工.md。
