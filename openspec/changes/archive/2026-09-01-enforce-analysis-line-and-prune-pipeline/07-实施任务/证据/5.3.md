# 任务 5.3 验证证据

## 交付物

- `openspec/schemas/delivery-change/templates/release-plan.md`：删除「现场快速资产」「日志、指标与观察窗口」「配置开关」三个恒空小节（含其目录树代码块与三张空表）；「Change 模式与交付结论」改写为「交付结论」并去掉 rehearsal 的 GO/NO-GO 分支；08 验收门禁勾选去掉 delivery/rehearsal 前缀。
- `openspec/schemas/delivery-change/templates/acceptance.md`：删除整个「Change 模式」小节（含 `change-mode.json` 来源说明与 rehearsal 原因栏）；前置门禁与最终结论的 delivery/rehearsal 分支表述一并清理。

## 验证

VC-030（`test/contracts.test.ts`）：

- 断言三个小节标题在 `release-plan.md` 中**不存在**。
- 断言仍保留 `## Spec Sync 与归档准备`、Spec Sync 表头 `| Delta Spec | Main Spec | Strict Validation | 结果 |`、两条门禁勾选（delta specs 已同步、cleanup 证据 PASS）与 `## 停止与回滚`——删的是恒空小节，不是门禁。
- 断言两份模板中都不再出现 `rehearsal` 与 `change-mode.json`，不留指向已移除概念的死引用。

`node --experimental-strip-types --test test/contracts.test.ts` → 10 passed / 0 failed。
