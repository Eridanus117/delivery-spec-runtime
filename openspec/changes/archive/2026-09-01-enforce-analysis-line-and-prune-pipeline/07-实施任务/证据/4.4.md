# 任务 4.4 验证证据

## 交付物

- `openspec/tools/delivery-control.ts`：
  - 删除 `Mode` 类型、`parseMode()`、`modePath()`、`updateSnapshotPath()`、`updateSnapshot()`、`updateDiagnose()` 及 `main()` 中的 `update snapshot` / `update diagnose` 两个分支。
  - `delivery` 作为显式默认写死为模块级常量 `const mode = "delivery" as const`，附注释说明 rehearsal 已退场、将来若需演练模式须重新立法而非恢复旧文件。
  - `guard` 的六个 operation 去掉全部 mode 分支：apply 不再判 rehearsal；acceptance 的「全部任务 verified」与 `requireReview` 由条件执行改为无条件执行；release 无条件 `requireAcceptance`（原 rehearsal 的非 PASS 结论分支一并删除）；sync / archive 去掉 rehearsal 禁令。输出仍带 `mode` 键，取值恒为 `delivery`，命令输出合同不变。
  - `init` 的 `--mode` 由必填改为可选，仅接受 `delivery`；传 `rehearsal` 显式报错。
  - 随之移除的未用导入：`relative`、`sep`、`JsonObject`。
- 删除 `openspec/contracts/change-mode.schema.json`；`public-allowlist.json` 同步移除该条目。

## 验证

`node --experimental-strip-types --test test/*.test.ts` → 63 passed / 0 failed。

VC-024（`test/control.test.ts` 两处）：

| 断言 | 结果 |
|---|---|
| `update snapshot` 与 `update diagnose` 均非零并报 `未知delivery-control命令` | 通过 |
| 新建 Change 不存在 `.delivery-update-snapshot.json` | 通过 |
| 批准新鲜度不依赖 snapshot：工件事后改动仍直接使 `change-plan` 批准 stale | 通过 |
| `init` 不产出 `change-mode.json` | 通过 |
| `init --mode rehearsal` 非零并说明该模式已随 `change-mode.json` 移除 | 通过 |
| 无 `change-mode.json` 时 `guard --operation apply` 放行且 `mode` 报 `delivery` | 通过 |
| **构造一份 rehearsal 的 `change-mode.json` 后，guard 的退出码与 stdout 与不存在时逐字节相等** | 通过 |
| **再把该文件写成非法 JSON，guard 退出码与 stdout 仍逐字节相等**（证明确实不再解析该文件，而不只是忽略其取值） | 通过 |

最后两条是「guard 行为与不存在时一致且不再解析该文件」的机器证据：若代码还在读它，非法 JSON 必然导致解析失败而非等价放行。
