# 任务 4.3 验证证据

## 交付物

`openspec/tools/delivery-lifecycle.ts` 的 `reopen`：

- 不再 `mkdirSync(lifecycle-history/<stamp>)`，不再 `copyFileSync` review / acceptance / readiness 三份状态，不再 `copyTree` 复制 08/09 目录。
- 不再 `atomicWriteJson(reopen-state.json)`。
- `--reason` / `--reopened-by` / `--reopened-at` 三个必填参数保留（仍是 reopen 的合法性输入），`reason` 与原归档目录名改为回显在 stdout 中。

移除理由写在代码注释里：全仓 grep 确认两者都没有任何 reader，纯留痕；而归档前的状态本来就在 git 历史里，再复制一份快照既不被机器消费，也不比 git 更可信。

## 验证

`node --experimental-strip-types --test test/lifecycle.test.ts` → 2 passed / 0 failed。

VC-006：`lifecycle reopen` 仍成功执行（目录从 archive 迁回 active、三份生命周期状态与 08/09 被清除、task-state 回落 `implemented_unverified`），并新增两条断言：

- 目标目录**不存在** `reopen-state.json`；
- 目标目录**不存在** `lifecycle-history/`。

原断言「`lifecycle-history/2026-08-30T12-03-00Z/implementation-review.json` 存在」被替换而非保留，避免留下指向已移除资产的死引用。
