# 任务 2.2 验证证据

## 交付物

- `openspec/tools/workflow-core.ts`：`WorkflowBinding` 增可选 `matterId`；`assertBinding` 的允许键扩该键但必需键不变，因此既有不带 `matterId` 的 binding 解析行为完全不变。新增导出 `intakeIdPattern` 与 `assertMatterId`，把 `matterId` 限定为 Intake 条目 id 形状（`INT-YYYYMMDD-NNN-slug`）——它要当目录名用，必须是路径安全的固定形状。
- `openspec/contracts/workflow-binding.schema.json`：同步 `matterId`（非必需），pattern 与代码一致。

## 验证

`node --experimental-strip-types --test test/workflow.test.ts` → 13 passed / 0 failed。

- 既有「workflow CLI 固定 Change binding 并拒绝静默切换」用例断言 `--change-root` 绑定产物仍是 `{schemaVersion, profileId, profileVersion}` 三键，逐字节不变，证明扩字段没有污染既有用法。
- VC-015 用例断言 `bind --intake-id` 产出的 binding 为四键且 `matterId` 等于条目 id，可被 `parseWorkflowBinding` 解析。
