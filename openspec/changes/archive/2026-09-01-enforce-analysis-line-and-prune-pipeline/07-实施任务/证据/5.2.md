# 任务 5.2 验证证据

## 交付物

`openspec/tools/delivery-control.ts`：

- `artifactPaths` 常量拆为 `v6ArtifactPaths` 与 `v5ArtifactPaths` 两张表，新增 `artifactPathsFor(root)` 按目录形状选表——存在 `03-现状/现状.md` 即 v6，否则 v5。这是「存量按各自版本解析、不迁移」在代码里的落点。
- `requiredBeforeAcceptance` 常量改为 `requiredBeforeAcceptanceFor(root)`，恒等于该 Change 结构下的全部工件。
- `artifactDigest` / `parseApprovals` / `approvalSet` / `approvalsInspect` / `inspect` / `guard` 六处调用点全部改为按 root 取表。

`test/helpers.ts`：`createArtifactTree` 的目录清单由 `03-业务现状` + `04-技术现状` 改为 `03-现状`；`control.test.ts`、`lifecycle.test.ts`、`station-authority.test.ts` 的 fixture 与工件清单同步改为 v6 八项。

## 验证

VC-028（`test/contracts.test.ts`「现状合并后门禁条目逐项可对应且一项不减」）：

| 断言 | 结果 |
|---|---|
| v6 工件集合 = v5 集合去掉 `business-current` / `technical-current`、并入 `current-state`，顺序与位置逐项可对应 | 通过 |
| v5 中除两份现状外的每一项在 v6 中都仍存在（无遗漏） | 通过 |
| 把除 `current-state` 外的工件全部批准后，`guard apply` **恰好**卡在 `current-state 批准状态为 pending` —— 证明它确实在 `requiredBeforeAcceptance` 里 | 通过 |
| 批准 `current-state` 后 `guard apply` 放行 | 通过 |
| 改动 `03-现状/现状.md` 正文后立刻 `current-state 批准状态为 stale` —— 证明 digest 计算确实接在该 artifact 上，承接了原两份各自的新鲜度校验 | 通过 |
| v6 结构下用旧工件名 `business-current` 执行 `approval set` 被拒（`批准参数非法`），避免两套命名并存 | 通过 |

关于「门禁条目数不减」的如实说明：工件**个数**由 9 变 8 是合并的直接结果，不可能不变；不减的是**校验项**——原本施加在两份现状上的 digest 新鲜度校验与批准门禁，逐项由 `current-state` 承接，且上表第 3、5 行分别给出了「在门禁清单里」与「digest 接在它身上」的机器证据。
