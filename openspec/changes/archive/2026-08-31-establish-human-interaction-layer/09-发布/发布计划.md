# 发布计划与快速资产

## Change 模式与交付结论

- 模式：delivery
- 结论：GO
- delivery 的 GO 表示 Change 已准备在功能分支完成 Spec Sync 和 Archive；不表示 PR 已创建或合并。

## 08 验收门禁

- [x] 当前 `acceptance-state.json` 为 PASS。
- [x] PASS run-id `20260831T1830Z-human-interaction` 覆盖必需 Requirement 和清理结果。

## 发布范围

| 仓库/能力 | Commit/版本 | 环境 | 责任方 |
|---|---|---|---|
| delivery-spec-runtime human-interaction（delivery-pilot skill + 四链合同） | `91111820ada3bef31e198f9d93c830778d92d516` 起的功能分支 | Runtime source repository | Runtime maintainer |

## 发布顺序与依赖

| 顺序 | 外部平台/人工动作 | 前置 | 成功标准 | 失败处置 |
|---:|---|---|---|---|
| 1 | 在功能分支归档 Change | Readiness guard allowed | Change 移入 `openspec/changes/archive/` | 保留 active Change，修复门禁 |
| 2 | 执行归档后 strict validation 与完整测试（LF 环境） | Archive 完成 | 所有校验通过 | 停止 PR 创建并记录失败证据 |
| 3 | 创建中文 PR（维护者已当轮授权 push 与 PR） | 最终验证通过 | PR 描述包含 Change、验证和风险 | 不推送、不创建 PR |

## 现场快速资产

本 Change 无生产部署，不创建外部 dashboards、alerts、config-switches 或 rollback 资产。

## 日志、指标与观察窗口

本 Change 只交付静态 skill 资产、manifest 数据、合同测试和文档；无运行时生产观察窗口。首个真实观察点为 3 事项复盘检查点（INT-20260831-007 记录）。

## 配置开关

无。

## 停止与回滚

- 停止条件：归档前任一 lifecycle guard、strict validation 或测试失败。
- 回滚门槛：消费仓升级后 skill 发现失败、第四条软链兼容破坏或交互合同与 spec 漂移。
- 回滚顺序：停止 PR/合并；删除 `.claude/` 资产并还原 manifest、测试与文档；重新执行 Review→Acceptance→Sync→Archive。
- 回滚后验证：运行 `render-commands.ts check`、全量合同测试和 `openspec validate --all --strict`。

## Spec Sync 与归档准备

| Delta Spec | Main Spec | Strict Validation | 结果 |
|---|---|---|---|
| `openspec/changes/establish-human-interaction-layer/specs/human-interaction/spec.md` | `openspec/specs/human-interaction/spec.md` | `openspec validate --all --strict` | PASS |

- [x] delta spec 已同步到 `openspec/specs/human-interaction/spec.md`，没有待应用差异。
- [x] Change 与主 specs strict validate 全部通过（11/11）。
- [x] cleanup 证据存在且结论 PASS。
- [x] 最终 PR 尚未创建；`archive-readiness.json` 将作 `prStarted=false` 声明。
