# 验收

## Change 模式

- 模式：delivery
- 模式来源：`change-info.json`
- rehearsal 原因与批准：不适用

## 前置门禁

- [x] `task-state.json` 全部 `verified`，07 投影一致。
- [x] `implementation-review.json` 为当前、PASS，且没有 OPEN finding。
- [x] 实现 commit、依赖版本和目标环境已锁定，06 必需场景可执行。

## 验收对象

| 仓库/制品 | 实际 Commit/版本 | 目标环境 | 依赖 |
|---|---|---|---|
| delivery-spec-runtime Intake workflow | `7c4804f851606ec5c671371c011d985b947049b1` | Runtime source root | Node `--experimental-strip-types`、Node `node:test` |

## Requirement 覆盖与结论

| Requirement/Scenario | Run ID | 证据路径 | 结果 | 偏差 |
|---|---|---|---|---|
| I-001 至 I-004：schema、init/inspect、DAG 和失败不写入 | `20260830-intake-contract` | `08-验收/runs/20260830-intake-contract/` | PASS | 无 |
| I-006、I-008 至 I-013：终态、reopen、legacy、安全与路径保护 | `20260830-intake-contract` | `08-验收/runs/20260830-intake-contract/` | PASS | 无 |
| Runtime 入口 `intake init/inspect` | `20260830-intake-contract` | `08-验收/runs/20260830-intake-contract/` | PASS | 无 |

## 验收运行

```text
08-验收/runs/20260830-intake-contract/
├── metadata.json
├── inputs/
├── outputs/
├── traces/
├── cleanup/
└── conclusion.md
```

运行使用合成输入和临时目录；没有写入真实业务数据、账号、环境、请求响应或 trace。

## Critical 与清理

- 未解决 critical：无。
- 清理结果：PASS；测试 finally 清理全部临时根，Runtime 入口冒烟文件已删除。

## 范围外回归观察

完整 `test/*.test.ts` 运行中，既有 `test/openspec-upgrade.test.ts` 的 `webcoding-spec` consumer fixture 返回 `runtimeStatus: 1`，其余 40 项通过。该失败不涉及 Intake 文件、Intake 命令或本 Change 的实现路径，因此不作为本 Change 的验收失败；仍保留为后续消费仓环境问题。

## 最终验收结论

- 结论：PASS
- 依据：Intake Change 专项合同测试、严格 Change 校验和 Runtime 入口真实 CLI 冒烟均通过；Review PASS、任务状态和 07 投影均一致；无未解决 critical。

## Acceptance State

正文和证据完成后执行 `delivery-lifecycle.ts acceptance write`，生成绑定当前 Review、task state、implementation commit 和本文件摘要的 `acceptance-state.json`。
