# 发布计划与快速资产

## Change 模式与交付结论

- 模式：delivery
- 结论：GO
- GO 仅表示本 Change 已准备完成 Spec Sync 和 Archive，不表示 PR 已创建或合并。

## 08 验收门禁

- [x] 当前 `acceptance-state.json` 为 PASS。
- [x] `20260830-intake-contract` 为 PASS，覆盖 Intake 必需场景并记录清理结果。

## 发布范围

| 仓库/能力 | Commit/版本 | 环境 | 责任方 |
|---|---|---|---|
| delivery-spec-runtime / Intake workflow | `7c4804f851606ec5c671371c011d985b947049b1` | Runtime source root | Runtime maintainer |

## 发布顺序与依赖

| 顺序 | 外部平台/人工动作 | 前置 | 成功标准 | 失败处置 |
|---:|---|---|---|---|
| 1 | 在 Runtime 功能分支提交 Change | Acceptance PASS | Change 与公共 Runtime 资产可审计 | 停止归档，保留失败证据 |
| 2 | 消费仓建立独立 Change 更新 Runtime gitlink | Runtime Archive 完成 | 消费仓 `runtime-check` 通过 | 按消费仓流程回滚 gitlink |

## 现场快速资产

本 Change 无外部部署；运行时命令由 Runtime submodule 和受管软链提供。

## 日志、指标与观察窗口

| 指标/日志 | 基线 | 告警/停止条件 | 观察窗口 |
|---|---|---|---|
| Intake CLI 退出码与 JSON 输出 | 专项合同测试全通过 | 非零退出码或 JSON 合同漂移 | 每次命令执行 |

## 配置开关

无。

## 停止与回滚

- 停止条件：`runtime-check`、Intake 合同测试或严格规格校验失败。
- 回滚门槛：Runtime 命令无法 fail closed，或公共候选包含未授权资产。
- 回滚顺序：恢复消费仓 Runtime gitlink，再恢复 Runtime 功能分支 Change。
- 回滚后验证：运行 `runtime-check`、渲染检查和 Intake 合同测试。

## Spec Sync 与归档准备

| Delta Spec | Main Spec | Strict Validation | 结果 |
|---|---|---|---|
| `openspec/changes/establish-intake-workflow/specs/intake-workflow/spec.md` | `openspec/specs/intake-workflow/spec.md` | `openspec validate --all --strict` | PASS |

- [x] delta spec 已同步到 `openspec/specs`，没有待应用差异。
- [x] Change 与主 specs strict validate 全部通过。
- [x] cleanup 证据存在且结论 PASS。
- [x] 最终 PR 尚未创建；`archive-readiness.json` 将声明 `prStarted=false`。

## Archive Readiness

使用 `delivery-lifecycle.ts readiness write` 绑定 Acceptance、本文件、Spec Sync 输入输出和 cleanup 摘要。只有 `guard archive` 返回 allowed 才能移动 Change。

## 归档后 PR

1. 在功能分支归档 Change。
2. 对归档后的仓库执行 final strict validation 和完整测试。
3. final validation 通过后才创建 PR。
4. PR 反馈若改变实现或规格，停止合并并受控 reopen，重新 Review→Acceptance→Sync→Archive。
5. 消费仓 gitlink 更新由各消费仓独立 Change 管理，不阻塞 Runtime Archive。
