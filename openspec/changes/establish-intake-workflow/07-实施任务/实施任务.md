# 实现任务拆分

> 状态真源：`task-state.json`。本文件由 `delivery-control.ts task render` 生成，只用于人工审阅；禁止反向解析复选框。

- [x] 1.1 [verified]
  - 交付物：openspec/contracts/intake-state.schema.json；openspec/intake/template.md；openspec/intake/stages.json
  - 验证：合法和非法 Intake fixture 通过 schema 校验
- [x] 2.1 [verified]
  - 交付物：openspec/tools/intake-control.ts 的 parser、DAG 和阶段输入检查
  - 验证：capture→triage→evidence→options→disposition 只允许合法后继，失败不写文件
- [x] 2.2 [verified]
  - 交付物：intake-control.ts 的 init、inspect、advance 和原子历史写入
  - 验证：临时项目仓真实 CLI 行为通过
- [x] 2.3 [verified]
  - 交付物：intake-control.ts 的 promote、hold、close、reopen 和 legacy inspect
  - 验证：双向来源、人工判断、终态和 legacy 缺口负向场景通过
- [x] 2.4 [verified]
  - 交付物：openspec/tools/runtime-entry.ts 的 intake 命令分派和路径守卫
  - 验证：runtime-check 后命令可用，Runtime submodule、越界路径和 dirty 状态 fail closed
- [x] 3.1 [verified]
  - 交付物：test/intake.test.ts 及合成 Intake fixtures
  - 验证：覆盖 I-001 至 I-013 的 schema、DAG、状态历史、legacy 和安全场景
- [x] 3.2 [verified]
  - 交付物：真实 CLI 探针和消费仓无写入回归测试
  - 验证：覆盖 stdout、stderr、退出码、Runtime/submodule/未授权路径摘要
- [x] 4.1 [verified]
  - 交付物：Intake README、Runtime workflow/consumer/governance 文档和 public-allowlist.json
  - 验证：文档示例使用 synthetic 数据，公共候选检查通过
- [x] 4.2 [verified]
  - 交付物：现有 Intake 条目的 legacy inspect 记录和迁移说明
  - 验证：旧文件不变，新命令不静默 Promote
