# 实现任务拆分

> 状态真源：`task-state.json`。本文件由 `delivery-control.ts task render` 生成，只用于人工审阅；禁止反向解析复选框。

- [x] 1.1 [verified]
  - 交付物：openspec/changes/fix-thorn-batch/05-改造方案/改造方案.md 的 D-03 决策日志
  - 验证：路径预算常量取值等于当前仓内实测最长路径 155，且注释写明 260 阈值与临时根前缀的推导
- [x] 2.1 [verified]
  - 交付物：openspec/tools/runtime-entry.ts 的 git() 在 win32 上带 -c core.longpaths=true
  - 验证：与 openspec-upgrade.ts 写侧写法一致；升级冒烟不再因超长路径误判 dirty
- [x] 2.2 [verified]
  - 交付物：openspec/tools/runtime-entry.ts 新增受管投影的 index 校验：未跟踪且被 ignore 即拒、index 模式为 120000 即拒
  - 验证：正负向断言各一组；已跟踪文件不被误伤
- [x] 2.3 [verified]
  - 交付物：openspec/tools/runtime-entry.ts 的 sourceRootFromScript() 找不到 manifest 时回落 findConsumerRoot()
  - 验证：投影副本在消费仓侧可作入口；未初始化目录仍 fail-closed 且文案指向 submodule 初始化
- [x] 2.4 [verified]
  - 交付物：openspec/tools/runtime-entry.ts 版本探测去掉 shell 选项
  - 验证：任一入口命令的合并输出中不含 DeprecationWarning
- [x] 2.5 [verified]
  - 交付物：openspec/tools/openspec-upgrade.ts 的 consumerSmoke 保留失败原因并写进报告；openspec/contracts/openspec-upgrade-report.schema.json 同步新增 failureReason
  - 验证：失败样本报告含非空原因文本；PASS 时为 null 且合同校验通过
- [x] 2.6 [verified]
  - 交付物：删除 bootstrap.ts / intake-control.ts / public-candidate.ts 的五个未用导入；删除 runtime-lib.ts 的 findUp、gitCommit、ensureInside 三个零调用导出
  - 验证：未用导入回扫为空；全量测试既有断言不变
- [x] 3.1 [verified]
  - 交付物：test/contracts.test.ts 的路径预算断言（T-PATH-1 / T-PATH-2）
  - 验证：当前工作树通过；合成超长路径被判超限并点名
- [x] 3.2 [verified]
  - 交付物：test/submodule.test.ts 的超长路径断言（T-LONG-1 / T-LONG-2）
  - 验证：干净超长路径不被判 dirty；真实修改仍被拒
- [x] 3.3 [verified]
  - 交付物：test/submodule.test.ts 的投影 index 断言（T-IGN-1 / T-IGN-2 / T-MODE-1）
  - 验证：ignore 吞投影被拒、已跟踪不误伤、120000 模式被拒
- [x] 3.4 [verified]
  - 交付物：test/submodule.test.ts 的入口回落与边界断言（T-ENTRY-1 / T-ENTRY-2）
  - 验证：投影副本可调用；孤立副本仍拒且文案正确
- [x] 3.5 [verified]
  - 交付物：test/openspec-upgrade.test.ts 的失败原因断言（T-SMOKE-1 / T-SMOKE-2）；test/control.test.ts 的输出洁净断言（T-QUIET-1）
  - 验证：失败原因非空且可读；正常评估无噪音；输出无弃用告警
- [x] 4.1 [verified]
  - 交付物：test/ 各临时根清理加 maxRetries 与 retryDelay 有限退避
  - 验证：bootstrap 测试连跑不再因 EPERM 中断
- [x] 5.1 [verified]
  - 交付物：实现收敛与全量验证证据
  - 验证：全量测试连跑三次全绿；validate --all --strict、render-commands check、runtime-check 均通过
