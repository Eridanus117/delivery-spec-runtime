# 验收

## Change 模式

- 模式：delivery
- 模式来源：未设置 `change-mode.json`，按合同使用 delivery。
- rehearsal 原因与批准：不适用。

## 前置门禁

- [x] `07-实施任务/实施任务.md` 九项任务全部为 `verified`。
- [x] 实际 commit、Node/OpenSpec 版本和目标环境已记录在 run metadata。
- [x] `06-测试方案/测试方案.md` 的必需场景、fixture、断言和清理规则均已执行。
- [x] `delivery-control.ts guard acceptance` 返回 `allowed: true`。

## 验收对象

| 仓库/制品 | 实际 Commit/版本 | 目标环境 | 依赖 |
|---|---|---|---|
| `Eridanus117/delivery-spec-runtime` | `82f283ba8b8b52e6169b2f48d2c29456b72eb0a7` | macOS arm64 本机合同环境 | Node `v26.7.0`、OpenSpec `1.10.0` 与 `1.11.0` |
| candidate Runtime | pin `1.11.0` | 隔离临时 Git 仓 | 确定性 renderer、候选 CLI |
| `agent-system` | `ccd54d7046b7fda4f08db48ab70556bef23ad206` | 隔离 Git 副本 smoke | candidate Runtime |
| `webcoding-spec` | `53ceeee7d11d300d20fe325c263fec5e8f871a95` | 隔离 Git 副本 smoke | candidate Runtime |
| `work-spec` | `b94dba4775704848daf56765ee5e31b04194e611` | 隔离 Git 副本 smoke | candidate Runtime |

## Requirement 覆盖与结论

| Requirement/Scenario | Run ID | 证据路径 | 结果 | 偏差 |
|---|---|---|---|---|
| current/candidate 必须在临时根生成 | `20260830T1151Z-upgrade-final` | `upgrade-evaluation/upgrade-report.json` | PASS | 无 |
| Commands 唯一结构化真源与漂移门禁 | 同上 | `outputs/runtime-tests.tap`、renderer check | PASS | 无 |
| upstream/current-local/candidate-local delta | 同上 | `upgrade-evaluation/upgrade-report.json` | PASS | 无 |
| current/candidate CLI JSON probes | 同上 | `upgrade-evaluation/upgrade-report.json` | PASS | 无 |
| 空白 fixture 与三消费仓隔离 smoke | 同上 | `upgrade-evaluation/upgrade-report.json` | PASS | 无 |
| README 采用与维护入口、Mermaid 图示 | 同上 | `outputs/runtime-tests.tap` | PASS | 无 |
| 1.11.0 候选完整门禁 | 同上 | `metadata.json`、`conclusion.md` | PASS | 无 |

## 验收运行

实际运行目录：`08-验收/runs/20260830T1151Z-upgrade-final/`

- `metadata.json`：代码 commit、环境、版本、命令状态和 setup retry。
- `inputs/commands.txt`：实际执行命令。
- `outputs/runtime-tests.tap`：Runtime 21 项合同测试原始输出。
- `upgrade-evaluation/upgrade-report.json`：脱敏升级、delta、probe、fixture、消费仓与清理报告。
- `public-candidate/candidate-report.json`：公开候选文件摘要与安全检查。
- `traces/requirement-coverage.md`：Requirement 与证据映射。
- `cleanup/cleanup.md`：临时根、真实仓和外部副作用清理结果。
- `conclusion.md`：本次 run 的独立结论。

## Critical 与清理

- 未解决 critical：无。
- 清理结果：临时根全部删除；三个真实消费仓 before/after digest 相同；升级请求临时文件删除；无外部临时仓库。

## 最终验收结论

结论: PASS

依据：真实 1.10.0→1.11.0 升级评估 PASS；Runtime 完整合同 21/21 PASS；renderer 0 drift；OpenSpec strict validate 2/2 PASS；空白 fixture 与三个消费仓隔离 smoke 全部 PASS；无产品偏差、无未解决 critical，清理成功。
