# 发布计划与快速资产

## Change 模式与发布结论

- 模式：delivery
- 结论：GO
- 授权：仓库维护者批准纠偏方案并要求继续；发布载体限定为功能分支与 PR #2。

## 08 验收门禁

- [x] `08-验收/验收记录.md` 结论为严格 PASS。
- [x] PASS run-id `20260830T1151Z-upgrade-final` 覆盖全部必需 Requirement 和清理结果。
- [x] Runtime 完整合同 21/21 PASS；真实升级、空白 fixture、三消费仓隔离 smoke 和 public candidate 全部通过。

## 发布范围

| 仓库/能力 | Commit/版本 | 环境 | 责任方 |
|---|---|---|---|
| `Eridanus117/delivery-spec-runtime` | PR #2 当前功能分支 head | GitHub `master` | Runtime 维护者 |
| OpenSpec pin | `1.10.0 → 1.11.0` | Runtime 消费仓后续 gitlink 升级 | Runtime/消费仓维护者 |
| Commands renderer 与升级评估器 | 本 Change 版本 | Runtime 维护和候选评估 | Runtime 维护者 |
| `/opsx-explore` 与 README | Mermaid 优先、写入前独立确认 | OMP 与 Runtime 文档消费者 | Runtime 维护者 |

本 Change 不更新三个消费仓的真实 gitlink；消费仓只在 Runtime PR 合并后按各自发布节奏升级。

## 发布顺序与依赖

| 顺序 | 外部平台/人工动作 | 前置 | 成功标准 | 失败处置 |
|---:|---|---|---|---|
| 1 | 推送 `change/establish-controlled-openspec-upgrades` | 严格验收 PASS | 远端功能分支存在，默认分支未直接写入 | 停止，不合并 PR |
| 2 | 审阅 PR #2 | PR head 包含 08/09 证据 | diff 仅包含本 Change 范围，必需检查针对同一 head 通过 | 修复后使用新 head 重新验证 |
| 3 | 合并 PR #2 | 当前 head 与通过检查的 head 一致 | `master` 包含受控升级流程和 1.11.0 pin | 停止消费仓升级；必要时 revert 合并提交 |
| 4 | 消费仓按需更新 gitlink | Runtime 默认分支稳定 | `runtime-check`、candidate CLI probe 与消费仓验证通过 | 回退 gitlink 到上一 commit |

## 现场快速资产

| 资产 | 路径/系统 | 用途 | 责任方 | 使用时点 |
|---|---|---|---|---|
| Runtime 合同输出 | `08-验收/runs/20260830T1151Z-upgrade-final/outputs/runtime-tests.tap` | 合并前核对行为 | Runtime 维护者 | PR 检查 |
| 升级报告 | `08-验收/runs/20260830T1151Z-upgrade-final/upgrade-evaluation/upgrade-report.json` | 审计版本、delta、probe、消费仓和清理 | Runtime 维护者 | PR 审阅、后续升级参照 |
| 公开候选报告 | `08-验收/runs/20260830T1151Z-upgrade-final/public-candidate/candidate-report.json` | 核对可公开文件及安全检查 | Runtime 维护者 | PR 检查 |
| Requirement 映射 | `08-验收/runs/20260830T1151Z-upgrade-final/traces/requirement-coverage.md` | 验收追踪 | Runtime 维护者 | 审阅与归档 |

## 日志、指标与观察窗口

| 指标/日志 | 基线 | 告警/停止条件 | 观察窗口 |
|---|---|---|---|
| Runtime 合同测试 | 21/21 PASS | 任意失败 | PR 合并前每个新 head |
| renderer check | 9 files、0 drift | 任一 missing/extra/modified | PR 合并前及每次 Command 变更 |
| 升级评估 | `result=PASS` | 版本、delta、probe、fixture、consumer 或 cleanup 任一失败 | 每次 OpenSpec pin 提升 |
| 消费仓摘要 | 三仓 before/after 相同 | 任一真实仓摘要或 Git 状态变化 | PR 合并前及首批消费仓升级 |

## 配置开关

无配置开关。OpenSpec 版本由 `runtime-manifest.json` 精确 pin；失败时不得提升 pin。

## 停止与回滚

- 停止条件：PR 必需检查失败；PR head 与已验证 head 不一致；renderer 漂移；真实升级报告非 PASS；任一消费仓 smoke 或摘要检查失败。
- 回滚门槛：1.11.0 在实际消费仓暴露未被 probe/合同覆盖的不兼容，或 Commands 渲染物无法由结构化真源重现。
- 回滚顺序：停止消费仓 gitlink 升级；回退消费仓 gitlink；revert Runtime 合并提交；以 1.10.0 重跑 Runtime 完整合同。
- 回滚后验证：renderer 0 drift；Runtime 完整合同通过；三个消费仓 `runtime-check` 通过；真实仓 clean。

## 外部发布记录

`release-not-required`：本 Change 仅通过 GitHub PR #2 发布源码和 Runtime gitlink 目标，不操作独立部署平台、运行环境配置或远程服务。PR 合并记录即外部发布记录；消费仓 gitlink 升级由各仓独立 Change 管理。

## 归档门禁

- [ ] PR #2 已合并，合并 commit 已记录。
- [ ] 首批消费仓按各自节奏升级并通过验证。
- [ ] `/opsx-verify` 无 critical。

归档门禁尚未满足；本 Change 随 PR 交付，但此时不得 sync 或 archive。
